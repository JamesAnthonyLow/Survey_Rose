<ul>
  <li>
    <%= question.body %>
    <% question.options.each do |option| %>
      <ul>
        <li><input type="radio" name="<%= question.id %>" value="<%= option.id %>"><%= option.choice %></li>
      </ul>
    <% end %>
  </li>
</ul>
